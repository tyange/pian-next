export type Burger = {
  Name: string;
  Brand: string;
  Description: string;
};

export type PageData = {
  PreviousPage: number;
  NextPage: number;
  CurrentPage: number;
  TotalPages: number;
};
